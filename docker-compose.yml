version: "3.4"
services:
  ecommerce-nodejs:
    image: node:19-alpine
    container_name: ecommerce-nodejs
    entrypoint: /bin/sh
    ports:
      - 8000:8000
    working_dir: /srv/app
    volumes:
      - type: bind
        source: ./
        target: /srv/app
    tty: true
